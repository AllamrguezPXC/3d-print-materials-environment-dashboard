# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Node
node_modules/
dist/
build/
.vite/

# Environment and secrets
.env
.env.*
!.env.example

# Assignment source document is marked "Confidencial — Solo uso interno" and
# this repo is public — kept local-only, intentionally excluded (not an
# oversight). Referenced by relative path from docs/ elsewhere in the repo.
docs/asignacion_react_fastapi.pdf

# SQLite and local data
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Playwright MCP verification artifacts (raw session logs, not evidence)
.playwright-mcp/

# Logs
*.log
